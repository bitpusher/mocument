namespace Mocument.ReverseProxyServer
{
    public enum SessionType
    {
        InvalidMimeType=-1,
        None,
        Record,
        Playback,
        Profile,
     
        Export
        
    }
}